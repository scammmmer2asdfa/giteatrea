{
  "name": "@repolens/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri shell for RepoLens (planned — see README.md in this folder).",
  "scripts": {
    "dev": "echo 'Desktop app not scaffolded yet. See apps/desktop/README.md.' && exit 1"
  }
}
