{
  "name": "@repolens/desktop",
  "version": "0.2.0",
  "private": true,
  "description": "Tauri shell for RepoLens.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
